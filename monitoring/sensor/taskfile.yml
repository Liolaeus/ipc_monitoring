version: "3"

includes:
  common:
    taskfile: ../taskfile.yml
    internal: true

tasks:
  build:
    desc: build sensor
    cmds:
      - task: common:build

  run:
    desc: run sensor
    cmds:
      - task: common:run
